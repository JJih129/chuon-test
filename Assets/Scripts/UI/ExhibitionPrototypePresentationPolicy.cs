public static class ExhibitionPrototypePresentationPolicy
{
    // Exhibition builds should guide players through objective panels, not dialogue beats.
    public static bool DialogueEnabled => false;
}
